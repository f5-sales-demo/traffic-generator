---
title: Smantellamento
description: >-
  terraform destroy per rimuovere tutte le risorse Azure, verifica
  dell'eliminazione del resource group con az CLI e pulizia dei file di stato
  locali
sidebar:
  order: 8
i18n:
  sourceHash: acaae5a3d897
  translator: machine
---

Al termine della sessione di laboratorio, distruggere tutte le risorse Azure per interrompere la fatturazione.

## Terraform Destroy

Dalla directory `terraform/`:

```bash
cd terraform/

terraform plan -destroy

terraform destroy
```

Terraform chiederà una conferma. Digitare `yes` per procedere.

## Verifica della pulizia

Confermare che tutte le risorse siano state rimosse:

```bash
RG_NAME=$(terraform output -raw resource_group_name)

az group show --name "${RG_NAME}" 2>&1 | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"

az resource list --resource-group "${RG_NAME}" -o table 2>/dev/null
```

## Pulizia dello stato locale

Rimuovere lo stato di Terraform e i file memorizzati nella cache:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## Conservare i risultati (Opzionale)

Se si desidera conservare i risultati della suite di traffico prima di distruggere la VM, copiarli localmente:

```bash
TGEN_IP=$(terraform output -raw public_ip)

# Copy all results to local machine
scp -r azureuser@${TGEN_IP}:/opt/traffic-generator/results/ ./traffic-results/

# Or copy just the latest run
LATEST=$(ssh azureuser@${TGEN_IP} 'ls -t /opt/traffic-generator/results/ | head -1')
scp -r azureuser@${TGEN_IP}:/opt/traffic-generator/results/${LATEST} ./traffic-results/
```

## Pulizia di F5 XC

Se si desidera rimuovere anche la configurazione F5 XC verso cui era indirizzato il generatore di traffico:

1. Eliminare l'HTTP load balancer
2. Eliminare l'origin pool
3. Eliminare eventuali policy WAF, configurazioni Bot Defense, policy API Security o configurazioni CSD create per i test

Queste risorse F5 XC non comportano costi Azure, ma dovrebbero essere rimosse quando l'ambiente demo non è più necessario.

:::tip
Per un utilizzo demo ricorrente, considerare di automatizzare l'intero ciclo di vita tramite script: `cd terraform && terraform apply` prima di una demo, eseguire le suite, quindi `terraform destroy` al termine. Il tier standard viene provisionato in 15-20 minuti, rendendo pratica la configurazione e lo smantellamento nello stesso giorno.
:::
