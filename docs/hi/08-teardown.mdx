---
title: टियरडाउन
description: >-
  सभी Azure संसाधनों को हटाने के लिए terraform destroy, az CLI से रिसोर्स ग्रुप
  डिलीशन सत्यापित करें, और स्थानीय स्टेट फ़ाइलें साफ़ करें
sidebar:
  order: 8
i18n:
  sourceHash: acaae5a3d897
  translator: machine
---

जब लैब सत्र पूर्ण हो जाए, तो बिलिंग रोकने के लिए सभी Azure संसाधनों को नष्ट करें।

## Terraform Destroy

`terraform/` डायरेक्टरी से:

```bash
cd terraform/

terraform plan -destroy

terraform destroy
```

Terraform पुष्टि के लिए संकेत देगा। आगे बढ़ने के लिए `yes` टाइप करें।

## क्लीनअप सत्यापित करें

पुष्टि करें कि सभी संसाधन हटा दिए गए हैं:

```bash
RG_NAME=$(terraform output -raw resource_group_name)

az group show --name "${RG_NAME}" 2>&1 | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"

az resource list --resource-group "${RG_NAME}" -o table 2>/dev/null
```

## स्थानीय स्टेट साफ़ करें

Terraform स्टेट और कैश्ड फ़ाइलें हटाएं:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## परिणाम सुरक्षित करें (वैकल्पिक)

यदि आप VM को नष्ट करने से पहले ट्रैफ़िक सूट के परिणाम रखना चाहते हैं, तो उन्हें स्थानीय रूप से कॉपी करें:

```bash
TGEN_IP=$(terraform output -raw public_ip)

# Copy all results to local machine
scp -r azureuser@${TGEN_IP}:/opt/traffic-generator/results/ ./traffic-results/

# Or copy just the latest run
LATEST=$(ssh azureuser@${TGEN_IP} 'ls -t /opt/traffic-generator/results/ | head -1')
scp -r azureuser@${TGEN_IP}:/opt/traffic-generator/results/${LATEST} ./traffic-results/
```

## F5 XC क्लीनअप

यदि आप उस F5 XC कॉन्फ़िगरेशन को भी हटाना चाहते हैं जिसे ट्रैफ़िक जनरेटर लक्षित कर रहा था:

1. HTTP लोड बैलेंसर हटाएं
2. ऑरिजिन पूल हटाएं
3. परीक्षण के लिए बनाई गई कोई भी WAF नीतियां, Bot उन्नत रक्षा कॉन्फ़िगरेशन, API सुरक्षा नीतियां, या CSD कॉन्फ़िगरेशन हटाएं

ये F5 XC संसाधन Azure लागत नहीं लगाते, लेकिन जब डेमो वातावरण की आवश्यकता न रहे तो इन्हें साफ़ किया जाना चाहिए।

:::tip
बार-बार होने वाले डेमो उपयोग के लिए, पूर्ण जीवनचक्र को स्क्रिप्ट करने पर विचार करें: डेमो से पहले `cd terraform && terraform apply`, सूट चलाएं, फिर बाद में `terraform destroy` करें। स्टैंडर्ड टियर 15-20 मिनट में प्रावधानित होता है, जिससे उसी दिन सेटअप और टियरडाउन व्यावहारिक बन जाता है।
:::
