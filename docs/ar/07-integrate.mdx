---
title: التكامل
description: >-
  Integration guide for the traffic generator with origin-server, F5 XC HTTP
  load balancer, CSD demo, and CDN simulator: architecture diagrams, targeting
  configuration, direct vs LB traffic comparison, and multi-component deployment
sidebar:
  order: 7
i18n:
  sourceHash: 542ecf0ca77e
  translator: machine
---

## البنية الكاملة

مولّد حركة المرور هو أحد المكونات في بيئة عرض توضيحي متعددة الطبقات. فيما يلي البنية الكاملة عند نشر جميع المكونات:

```
Traffic Generator -> F5 XC HTTP LB (WAF/Bot/API/CSD) -> Origin Server
                         |
               CDN Simulator (optional)
```

```mermaid
graph LR
    TG[Traffic Generator VM] -->|Attack Traffic<br/>HTTPS| XCHLB[F5 XC HTTP LB]
    XCHLB -->|WAF / Bot / API / CSD| ORIGIN[Origin Server VM<br/>Juice Shop, DVWA,<br/>VAmPI, httpbin, whoami]
    CDN[CDN Simulator<br/>optional] -->|Cached Requests| XCHLB
    TG -.->|Direct Baseline<br/>HTTP optional| ORIGIN
```

يتم نشر وتكوين كل مكون بشكل مستقل عبر Terraform. يستهدف مولّد حركة المرور اسم النطاق المؤهل بالكامل (FQDN) لموازن الأحمال F5 XC، وليس خادم الأصل مباشرة.

## التكامل مع خادم الأصل

يوفر [خادم الأصل](https://f5xc-salesdemos.github.io/origin-server/) تطبيقات الخلفية التي تستهدفها مجموعات الهجوم الخاصة بمولّد حركة المرور:

| مجموعة حركة المرور | تطبيق الأصل | المسار |
|---|---|---|
| api-attacks | VAmPI | `/vampi/` |
| bot-simulation | جميع التطبيقات | جميع المسارات |
| cdn-load-testing | CDN Simulator | نقطة نهاية CDN |
| crapi-exploits | crAPI | `/crapi/` |
| csd-demo-attacks | CSD Demo | `/csd-demo/` |
| dvga-exploits | DVGA | `/dvga/` |
| dvwa-exploits | DVWA | `/dvwa/` |
| javascript-exploits | CSD Demo | `/csd-demo/` |
| juice-shop-exploits | Juice Shop | `/juice-shop/` |
| mitre-attack | جميع التطبيقات | جميع المسارات |
| owasp-scanning | جميع التطبيقات | جميع المسارات |
| performance-testing | جميع التطبيقات | جميع المسارات |
| reconnaissance | جميع التطبيقات | جميع المسارات |
| restaurant-exploits | Restaurant API | `/restaurant/` |
| ssl-scanning | F5 XC LB (ليس الأصل مباشرة) | غير متاح |
| traffic-generation | جميع التطبيقات | جميع المسارات |
| web-app-attacks | Juice Shop, DVWA | `/juice-shop/`, `/dvwa/` |

### ترتيب النشر

1. انشر **خادم الأصل** أولاً -- فهو يوفر تطبيقات الخلفية
2. قم بتكوين **موازن أحمال F5 XC HTTP** مع خادم الأصل كمجمع أصل
3. أرفق **سياسات WAF وBot Defense وAPI Security وCSD** بموازن الأحمال
4. انشر **مولّد حركة المرور** مع تعيين `target_fqdn` إلى نطاق موازن أحمال F5 XC

### تكوين الاستهداف

يربط ملف `config.env` الخاص بمولّد حركة المرور بينه وبين بقية البنية:

```bash
# Target the F5 XC load balancer (traffic passes through security policies)
TARGET_FQDN=demo.example.com

# Optional: target the origin server directly (bypasses F5 XC)
TARGET_ORIGIN_IP=20.10.5.100
```

عند تعيين `TARGET_FQDN`، ترسل جميع نصوص المجموعات حركة المرور إلى `https://<TARGET_FQDN>/...`. يستقبل موازن أحمال F5 XC الطلبات، ويطبق سياسات الأمان، ويعيد توجيه حركة المرور المسموح بها إلى خادم الأصل.

## التكامل مع عرض CSD التوضيحي

مجموعة `javascript-exploits` مصممة خصيصًا لعرض الدفاع من جانب العميل (Client-Side Defense) التوضيحي على خادم الأصل. تتحقق هذه المجموعة من وظائف المرحلة الثانية من CSD:

**سير عمل المرحلة الثانية:**

1. يستضيف خادم الأصل صفحة عرض CSD التوضيحي في المسار `/csd-demo/`
2. يحقن F5 XC CSD كود JavaScript الخاص بالمراقبة في الصفحة
3. تحاول مجموعة javascript-exploits الخاصة بمولّد حركة المرور:
   - حقن نصوص مضمّنة تحاكي برامج سرقة بيانات Magecart
   - تعديل عناصر DOM لإعادة توجيه إرسال النماذج
   - تحميل كود JavaScript غير مصرح به من جهات خارجية
4. يكتشف F5 XC CSD هذه التعديلات ويعرضها في لوحة معلومات CSD

لاستخدام مجموعة javascript-exploits:

```bash
# Ensure CSD is enabled on the F5 XC HTTP LB for the /csd-demo/ path
# Then run the suite
/opt/traffic-generator/suites/runner.sh javascript-exploits
```

## التكامل مع محاكي CDN

عند نشر محاكي CDN، تضيف البنية طبقة تخزين مؤقت:

```
Traffic Generator -> CDN Simulator -> F5 XC HTTP LB -> Origin Server
```

يقع محاكي CDN أمام موازن أحمال F5 XC، حيث يخزّن الاستجابات مؤقتًا ويضيف ترويسات شبيهة بـ CDN. لتوجيه حركة المرور عبر CDN:

```bash
# Set TARGET_FQDN to the CDN Simulator's endpoint instead of F5 XC directly
TARGET_FQDN=cdn.demo.example.com
```

هذا مفيد لتوضيح كيفية تعامل F5 XC مع حركة المرور القادمة عبر CDN، بما في ذلك:

- تحديد عنوان IP الحقيقي للعميل خلف ترويسات وكيل CDN
- تطبيق قواعد WAF على الطلبات التي ربما تم تعديلها بواسطة CDN
- تصنيف Bot Defense عندما يعدّل CDN بصمات المتصفح

## مقارنة حركة المرور المباشرة مع حركة المرور عبر موازن الأحمال

يدعم مولّد حركة المرور إرسال حركة المرور عبر F5 XC ومباشرة إلى الأصل. توضح هذه المقارنة قيمة ميزات أمان F5 XC:

### عبر F5 XC (الافتراضي)

```bash
# Traffic goes: Generator -> F5 XC LB -> Origin
TARGET_FQDN=demo.example.com /opt/traffic-generator/suites/runner.sh web-app-attacks
```

المتوقع: يحظر WAF حمولات حقن SQL وXSS وحقن الأوامر. تعرض لوحة معلومات أحداث الأمان الطلبات المحظورة مع تفاصيل الانتهاكات.

### مباشرة إلى الأصل (خط الأساس)

```bash
# Traffic goes: Generator -> Origin (no security layer)
TARGET_FQDN=20.10.5.100 /opt/traffic-generator/suites/runner.sh web-app-attacks
```

المتوقع: تصل جميع الحمولات إلى تطبيقات الأصل دون تصفية. يعالج Juice Shop وDVWA حمولات الهجوم. هذا يوضح ما يحدث بدون حماية F5 XC.

### سير العرض التوضيحي جنبًا إلى جنب

لعرض توضيحي مقنع، قم بتشغيل نفس المجموعة بكلتا الطريقتين:

1. شغّل `web-app-attacks` مباشرة ضد الأصل -- أظهر نجاح الهجمات
2. شغّل `web-app-attacks` عبر F5 XC -- أظهر حظر الهجمات
3. افتح لوحة معلومات أحداث أمان F5 XC لعرض الطلبات المحظورة
4. قارن نتائج `meta.json` للمجموعة: التشغيل المباشر يظهر المزيد من "passed" (نجحت الهجمات)، والتشغيل عبر موازن الأحمال يظهر المزيد من "failed" (تم حظر الهجمات)

```bash
TGEN_IP=$(terraform output -raw public_ip)
ORIGIN_IP="20.10.5.100"
LB_FQDN="demo.example.com"

# Run 1: Direct (baseline)
ssh azureuser@${TGEN_IP} "TARGET_FQDN=${ORIGIN_IP} /opt/traffic-generator/suites/runner.sh web-app-attacks"

# Run 2: Through F5 XC
ssh azureuser@${TGEN_IP} "TARGET_FQDN=${LB_FQDN} /opt/traffic-generator/suites/runner.sh web-app-attacks"

# Compare results
ssh azureuser@${TGEN_IP} 'for d in $(ls -t /opt/traffic-generator/results/ | head -2); do echo "=== $d ==="; cat /opt/traffic-generator/results/$d/meta.json; echo; done'
```

## نشر Terraform متعدد المكونات

عند نشر بيئة المختبر الكاملة، استخدم مساحات عمل أو أدلة Terraform منفصلة لكل مكون:

```bash
# 1. Deploy origin server
cd origin-server
terraform apply -var="subscription_id=YOUR_SUB_ID"
ORIGIN_IP=$(terraform output -raw public_ip)

# 2. Configure F5 XC (manual or via separate Terraform)
# Create origin pool -> HTTP LB -> attach WAF/Bot/API/CSD policies
# LB_FQDN=demo.example.com

# 3. Deploy traffic generator targeting the F5 XC LB
cd ../traffic-generator
terraform apply \
  -var="subscription_id=YOUR_SUB_ID" \
  -var="target_fqdn=demo.example.com" \
  -var="target_origin_ip=${ORIGIN_IP}"

# 4. Generate traffic
TGEN_IP=$(terraform output -raw public_ip)
ssh azureuser@${TGEN_IP} '/opt/traffic-generator/suites/runner.sh web-app-attacks'
```
