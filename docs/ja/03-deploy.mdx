---
title: デプロイ
description: >-
  トラフィックジェネレーター Azure VM の Terraform IaC デプロイ — Ubuntu 24.04 搭載の F シリーズ
  コンピューティング最適化 VM、カーネルチューニング、50 以上のセキュリティツール、cloud-init でプロビジョニングされる 19
  のトラフィックスイート。すべての構成ファイルは terraform/ ディレクトリにあります。
sidebar:
  order: 3
i18n:
  sourceHash: bc522cf1cb30
  translator: machine
---

すべての Terraform ファイルは [`terraform/`](https://github.com/f5xc-salesdemos/traffic-generator/tree/main/terraform) ディレクトリにあります。リポジトリをクローンして直接デプロイしてください：

```bash
git clone https://github.com/f5xc-salesdemos/traffic-generator.git
cd traffic-generator/terraform
cp terraform.tfvars.example terraform.tfvars
# terraform.tfvars を Azure サブスクリプション ID とターゲット FQDN で編集
```

## Terraform 構成

### Terraform ファイル構造

terraform ディレクトリには、デモリソース標準に従った 9 つのファイルが含まれています：

- `versions.tf` -- Terraform およびプロバイダーのバージョン制約 (azurerm ~> 4.0, azuread ~> 3.0)
- `providers.tf` -- Azure RM および Azure AD プロバイダー構成
- `data.tf` -- デプロイヤー自動解決用の Azure AD データソース
- `locals.tf` -- デプロイヤー解決、Azure Cloud Adoption Framework リソース命名、標準タグ
- `main.tf` -- リソースグループ（`rg-traffic-generator-{environment}-{deployer}` という名前）
- `variables.tf` -- すべての入力変数（必須 2、オプション 9）と Azure AD によるデプロイヤー自動解決
- `network.tf` -- VNet (10.201.0.0/16)、サブネット、パブリック IP、NSG（ポート 22 SSH）、NIC
- `vm.tf` -- templatefile() による cloud-init 付き Ubuntu 24.04 VM
- `outputs.tf` -- 17 の出力：すべてのデモリソースで共有される 15 の標準出力（deployer、public_ip、private_ip、ssh_command、resource_group_name、vm_name、nsg_name、vnet_name、subnet_id、component、environment、resource_group_id、vm_id、nsg_id、location）と 2 つのコンポーネント固有出力（target_fqdn、status_check）

### プロバイダーと変数

`main.tf` は Azure プロバイダーを構成します：

```hcl file=./_data/main.tf
```

`variables.tf` はすべての設定可能なパラメーターを定義します。`vm_size` のデフォルトは `Standard_F16s_v2`（16 vCPU コンピューティング最適化）です。スケーリングのガイダンスについては [VM サイジング](#vm-サイジング) を参照してください：

```hcl file=./_data/variables.tf
```

### ネットワークインフラストラクチャ

`network.tf` は VNet、サブネット、パブリック IP、NSG（ポート 22 SSH）、NIC を作成します：

```hcl file=./_data/network.tf
```

### Cloud-Init 付き仮想マシン

`vm.tf` は Ubuntu 24.04 VM を作成し、ターゲットとツーリング変数を含む cloud-init テンプレートを渡します：

```hcl file=./_data/vm.tf
```

### Cloud-Init プロビジョニング

`cloud-init.yaml` は、4 ラウンドのオリジン耐久テストを含む 48 時間以上の継続的な負荷テストで検証された本番環境向け構成です。リトライロジック付きのヘルパーライブラリ、`/var/log/cloud-init-progress.log` への進捗ログ、9 つのプロビジョニングフェーズ、条件付きステータスレポート（`status.json` の ready/degraded）を使用します。プロビジョニング内容：

- **カーネルチューニング** -- `somaxconn=131072`、`tcp_max_tw_buckets=4000000`、`tcp_fin_timeout=5`、64 MiB ソケットバッファ、`file-max=4194304`
- **NIC 最適化** -- 全コアにわたる RPS/RFS、ethtool によるリングバッファ最大化、THP=always
- **APT パッケージ** -- nikto、nmap、masscan、sqlmap、hydra、medusa、ncrack、tshark、hping3、tcpdump、netcat、ngrep、iperf3、mtr、sslscan、socat、dirb、whatweb、wrk、hey、vegeta、ethtool
- **GitHub リリース** -- nuclei、dalfox、ffuf、gobuster、feroxbuster、subfinder、httpx、amass
- **Python パッケージ** -- mitmproxy、sslyze、dnsrecon、fierce、theHarvester、playwright、scapy、impacket、arjun
- **Node.js 20.x** -- ボットシミュレーション用のステルスプラグイン付き Puppeteer
- **ブラウザ自動化** -- ヘッドレスブラウザテスト用の Playwright Chromium
- **Git クローン** -- testssl.sh、recon-ng、spiderfoot、SecLists、waf-bypass、PayloadsAllTheThings
- **スイートデプロイ** -- このリポジトリから `/opt/traffic-generator/suites/` に 19 のトラフィックスイートをクローン
- **設定ファイル** -- `TARGET_FQDN`、`TARGET_ORIGIN_IP`、`CRAPI_PORT` を含む `config.env`

cloud-init は Terraform テンプレート変数 `${target_fqdn}`、`${target_origin_ip}`、`${tool_tier}` を使用します。`$NPROC` のようなシェル変数は、Terraform templatefile 内で `$$NPROC` としてエスケープされます。

```yaml file=./_data/cloud-init.yaml
```

### 出力

`outputs.tf` は、パブリック/プライベート IP、SSH コマンド、リソース識別子、コンポーネント固有の値（target_fqdn、status_check）を含む 17 の出力を公開します：

```hcl file=./_data/outputs.tf
```

### 変数ファイルの例

`terraform.tfvars.example` を `terraform.tfvars` にコピーして値を入力してください。`.gitignore` は認証情報のコミットを防ぐために `terraform.tfvars` を除外しています：

```hcl file=./_data/terraform.tfvars.example
```

`subscription_id` を Azure サブスクリプション ID（`az account show --query id -o tsv` で取得）に、`target_fqdn` を F5 XC ロードバランサーのドメインに置き換えてください。

## デプロイ

```bash
terraform init

terraform plan

terraform apply
```

デプロイが成功すると、Terraform はパブリック IP、SSH コマンド、ターゲット FQDN を出力します。

## VM サイジング

F シリーズのコンピューティング最適化 VM は、この CPU バウンドなトラフィック生成ワークロードに推奨されます。cloud-init のカーネルチューニングとツールの並行処理は、vCPU 数に応じて自動的にスケールします。

| VM SKU | vCPU | RAM | ネットワーク | ユースケース |
|---|---|---|---|---|
| Standard_F4s_v2 | 4 | 8 GiB | 10 Gbps | 軽量ラボ/デモ |
| Standard_F8s_v2 | 8 | 16 GiB | 12.5 Gbps | 標準デモ |
| Standard_F16s_v2 | 16 | 32 GiB | 12.5 Gbps | 負荷テスト（デフォルト） |
| Standard_F32s_v2 | 32 | 64 GiB | 16 Gbps | 高負荷ベンチマーク |
| Standard_D16s_v3 | 16 | 64 GiB | 12.5 Gbps | RAM 集約型ワークロード |

:::tip
F16s_v2 は、CDN F16s_v2 および F32s_v2 ターゲットに対する A/B/C/D ベンチマークテストにより、最適なコストパフォーマンスオプションとして検証されました。完全な結果については [benchmarks/](https://github.com/f5xc-salesdemos/traffic-generator/tree/main/benchmarks) を参照してください。
:::

## デプロイ後の検証

Cloud-init のプロビジョニングは、**standard ティアで 15〜20 分**、**full ティアで約 25 分**かかります。VM には SSH で即座にアクセスできますが、cloud-init が完了するまでツールのインストールが続行されます。

### Cloud-Init の完了を待つ

VM に SSH 接続し、プロビジョニングの完了を待ちます：

```bash
ssh azureuser@$(terraform output -raw public_ip)

cloud-init status --wait
```

完了時の期待される出力：

```
status: done
```

### ステータスファイルの確認

cloud-init スクリプトは完了時にステータスファイルを書き出します：

```bash
cat /opt/traffic-generator/status.json
```

期待される出力：

```json
{
  "status": "ready",
  "tier": "standard",
  "target_fqdn": "demo.example.com",
  "completed": "2026-04-25T14:30:00Z",
  "tools_installed": true
}
```

### 重要なツールの検証

各重要なツールが PATH 上で利用可能であることを確認するための検証ループを実行します：

```bash
for tool in nikto sqlmap nuclei dalfox ffuf gobuster feroxbuster nmap masscan \
  hydra medusa ncrack sslscan tshark hping3 mitmproxy sslyze subfinder httpx \
  amass arjun mtr whatweb dirb socat netcat ngrep iperf3 whois dig wrk hey; do
  if command -v "$tool" > /dev/null 2>&1; then
    echo "OK: $tool"
  else
    echo "MISSING: $tool"
  fi
done
```

### スイートランナーの検証

ランナーとスイートがインストールされていることを確認します：

```bash
ls /opt/traffic-generator/suites/runner.sh
ls /opt/traffic-generator/suites/*/

/opt/traffic-generator/suites/runner.sh web-app-attacks --dry-run
```

## Terraform 出力

| 出力 | 説明 | 例 |
|---|---|---|
| `deployer` | 解決されたデプロイヤー識別子 | `jsmith` |
| `public_ip` | トラフィックジェネレーター VM のパブリック IP アドレス | `20.12.78.200` |
| `private_ip` | VM のプライベート IP アドレス | `10.201.0.4` |
| `ssh_command` | VM に接続するための SSH コマンド | `ssh azureuser@20.12.78.200` |
| `resource_group_name` | リソースグループ名 | `rg-traffic-generator-dev-jsmith` |
| `resource_group_id` | リソースグループの Azure リソース ID | `/subscriptions/.../resourceGroups/rg-traffic-generator-dev-jsmith` |
| `vm_name` | 仮想マシン名 | `vm-traffic-generator-dev-jsmith` |
| `vm_id` | 仮想マシンの Azure リソース ID | `/subscriptions/.../virtualMachines/vm-traffic-generator-dev-jsmith` |
| `nsg_name` | ネットワークセキュリティグループ名 | `nsg-traffic-generator-dev-jsmith` |
| `nsg_id` | ネットワークセキュリティグループの Azure リソース ID | `/subscriptions/.../networkSecurityGroups/nsg-traffic-generator-dev-jsmith` |
| `vnet_name` | 仮想ネットワーク名 | `vnet-traffic-generator-dev-jsmith` |
| `subnet_id` | サブネットの Azure リソース ID | `/subscriptions/.../subnets/snet-traffic-generator-dev-jsmith` |
| `component` | コンポーネント識別子 | `traffic-generator` |
| `environment` | デプロイ環境 | `dev` |
| `location` | Azure リージョン | `eastus2` |
| `target_fqdn` | トラフィックスイート用に設定されたターゲット FQDN | `demo.example.com` |
| `status_check` | プロビジョニングステータスを確認するコマンド | `ssh azureuser@20.12.78.200 cat /opt/traffic-generator/status.json` |

任意の出力を取得：

```bash
terraform output -raw public_ip
terraform output -raw ssh_command
```

## アップストリームコンポーネントからの接続

トラフィックジェネレーターは、オリジンサーバーの前段にある F5 XC HTTP ロードバランサーをターゲットとします。まずオリジンサーバーをデプロイし、F5 XC を構成してから、`target_fqdn` をロードバランサーの FQDN に設定します：

```bash
# origin-server をデプロイし、F5 XC HTTP ロードバランサーを作成した後：
cat > terraform.tfvars <<EOF
subscription_id  = "your-subscription-id"
target_fqdn      = "your-xc-load-balancer.example.com"
target_origin_ip = "$(cd ../../origin-server/terraform && terraform output -raw public_ip)"
EOF
```

| 必須変数 | ソース | 説明 |
|---|---|---|
| `target_fqdn` | F5 XC コンソール | オリジンサーバーを保護する HTTP ロードバランサーの FQDN |
| `target_origin_ip` | オリジンサーバーの `public_ip` 出力 | バイパステスト用の直接オリジン IP（オプション） |

ツールの完全なリファレンスについては [ツールカタログ](../04-tool-catalog/) に、トラフィック生成を開始するには [スイート](../05-suites/) に進んでください。
